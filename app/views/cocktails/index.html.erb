

<div class="banner">
  <div class="container">
    <h1><strong>Cocktails for you!</strong></h1>
  </div>
  <div class="container">
  </div>

<div class="container">
  <div class="row">
    <% @cocktails.each do |c| %>
    <div class="col-4">
        <div class="card" style="width: 18rem;">
          <img class="card-img-top" src='<%= cloudinary_url c.photo %>' alt="Card image cap">
          <div class="card-body">
            <h5 class="card-title"><%= c.name %></h5>
            <%= link_to 'See details', cocktail_path(c)  %>
<%= link_to 'delete', cocktail_path(c), method: :delete, data: {confirm: 'Are you sure?'}, class: 'btn btn-danger'%>
          </div>
        </div>
      </div>
    <% end %>
</div>
</div>
