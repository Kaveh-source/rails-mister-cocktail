

<div class="banner">
  <div class="container">
    <h1><strong>Kaveh's Cocktails!</strong></h1>
  </div>
  <div class="container">
  </div>

<div class="container">
  <div class="row">
    <% @cocktails.each do |c| %>
    <div class="col d-flex align-items-stretch">
        <div class="card mt-3 align-items-center" style="width: 15rem">

        <%= cl_image_tag c.photo.url %>
          <div class="card-body">
            <h5 class="card-title"><%= c.name %></h5>
            <%= link_to 'See details', cocktail_path(c)  %>
          </div>
        </div>
    </div>


    <% end %>
</div>
</div>
